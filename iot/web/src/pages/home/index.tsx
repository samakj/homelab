/** @format */
import { Main } from '@/components/page-structure';

import { HomePagePropsType } from './types';

const Home: React.FunctionComponent<HomePagePropsType> = ({}) => {
  return <Main>home</Main>;
};

export default Home;

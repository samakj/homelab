/** @format */
import { LocalStorageType } from './types';

export const LocalStorage: LocalStorageType = {
  routerDevices: {},
  routerConnectedDevices: {},
  scrapedDevices: {},
  deviceWebsockets: {},
  devices: {},
};

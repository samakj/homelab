{
    "auth": {
        "username": "${auth.users.devices_scraper.username}",
        "password": "${auth.users.devices_scraper.password}"
    },
    "urls": {
        "authorisation_api": "http://${containers.apis.authorisation}:${ports.apis.authorisation}",
        "iot_api": "http://${containers.apis.iot}:${ports.apis.iot}"
    }
}
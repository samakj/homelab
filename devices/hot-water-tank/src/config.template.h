#define HOSTNAME "${devices.hot_water_tank.hostname}"
#define IP_ADDRESS "${devices.hot_water_tank.ip}"

#define PURLEY_PARK_SSID "${wifi.credentials.purley_park.ssid}"
#define PURLEY_PARK_PASSWORD "${wifi.credentials.purley_park.password}"

#define THE_VALE_SSID "${wifi.credentials.the_vale.ssid}"
#define THE_VALE_PASSWORD "${wifi.credentials.the_vale.password}"

#define NTP_SERVER "${ntp.server}"

#define OTA_PASSWORD "${devices.hot_water_tank.ota_password}"

#define DHT_PIN 13
#define DS18B20_PIN 14
#define DS18B20_ADDRESS "28c48716a813cfc"
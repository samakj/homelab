#define HOSTNAME             "${devices.sandbox.hostname}"
#define IP_ADDRESS           "${devices.sandbox.ip}"

#define PURLEY_PARK_SSID     "${wifi.credentials.purley_park.ssid}"
#define PURLEY_PARK_PASSWORD "${wifi.credentials.purley_park.password}"

#define VALE_PARK_SSID       "${wifi.credentials.the_vale.ssid}"
#define VALE_PARK_PASSWORD   "${wifi.credentials.the_vale.password}"

#define NTP_SERVER           "${ntp.server}"

#define OTA_PASSWORD         "${devices.sandbox.ota_password}"

#define DHT_PIN              13